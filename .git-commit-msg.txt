script: lower i64 min/max + document-vector-sort (spotwork needs)

spotwork's .kotoba port uses i64 `min`/`max` for working-hour overlap and
`document-vector-sort` (a deterministic sort over a [:vector :document] via
the existing docCompare total order) for cohort ranking.

- emit-call: `min`->`i64(a<b?a:b)`, `max`->`i64(a>b?a:b)` (BigInt-safe
  ternary, since i64 are JS BigInt), `document-vector-sort`->`docVectorSort(...)`
- operand typing: add `min`/`max` to the i64 binary set; add
  `document-vector-sort` as a 1-document-arg -> :document head
- prelude: `const docVectorSort=(v)=>{...out.sort(docCompare)...}`
- regen the JVM-written parity goldens (development-only fixture writer;
  the parity test itself runs JVM-free on nbb and now reports
  `SCANNED 99 failed 0`): the prelude gained the one `docVectorSort` line,
  so every fixture's emitted `.mjs` gained it too.

nbb parity: SCANNED 99 failed 0. Paired with sema (min 2 max 2 +
document-vector-sort 1 -> KIR) and KIR (osaho #84/#85 interpreter cases).